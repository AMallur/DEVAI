export default function About() {
  return (
    <section id="about">
      <div className="container about-box">
        <p className="section-label">{"// about"}</p>
        <h2 className="section-title">Why Aeryn exists</h2>
        <p className="section-sub">
          Instead of scattering products across separate sites and repos,
          everything gets built, shipped, and listed from this one base —
          a single front door to GoState, FableAgent, GAIA, RecallDock, and
          every project that follows.
        </p>
      </div>
    </section>
  );
}
